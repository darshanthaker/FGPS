conditioning:
  method: fgps
  params:
    scale: 0.4 # only used if scale_schedule is 'fixed'.
    # Frequency schedule.
    start_bound: 10
    end_bound: 75
    schedule: linear
    # Step size schedule.
    scale_start: 3.0
    scale_end: 0.1
    scale_schedule: linear ##fixed

data:
  name: imagenet
  root: ./data/imagenet_samples/

measurement:
  operator:
    name: motion_blur
    kernel_size: 61
    intensity: 0.5 

  noise: 
    name: gaussian
    sigma: 0.05
